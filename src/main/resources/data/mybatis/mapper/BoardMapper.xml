<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mybatis.mapper.BoardMapper">

	<insert id="BoardInsert" parameterType="com.spring.datetree.Vo.BoardVo">
		insert into board values( #{board_seq.nextval},#{user_id},#{title},#{content},#{count},#{likecount},sysdate)
	</insert>
</mapper>